apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: nagp-assignment
data:
  PORT: "8080"
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "finopsdb"
  DB_USER: "appuser"
  DB_POOL_MAX: "20"
  DB_POOL_IDLE_TIMEOUT_MS: "10000"
  DB_POOL_CONNECTION_TIMEOUT_MS: "3000"

